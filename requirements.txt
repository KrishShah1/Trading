# Data
yfinance>=0.2.40      # historical OHLC + fundamentals (Python analog of yahoo-finance2)
pandas>=2.1
numpy>=1.26
pyarrow>=15.0         # parquet cache for prices/fundamentals
lxml>=5.0             # pandas.read_html for the S&P 500 constituent table
pyyaml>=6.0           # config/*.yaml

# UI (local lab only — disposable, not ported to the JS suite)
streamlit>=1.30
plotly>=5.20
